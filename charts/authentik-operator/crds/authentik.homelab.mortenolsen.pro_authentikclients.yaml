---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: authentikclients.authentik.homelab.mortenolsen.pro
spec:
  group: authentik.homelab.mortenolsen.pro
  names:
    kind: AuthentikClient
    listKind: AuthentikClientList
    plural: authentikclients
    singular: authentikclient
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AuthentikClient is the Schema for the authentikclients API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of AuthentikClient
            properties:
              authentikUrl:
                description: |-
                  authentikUrl is the base URL for the Authentik server API and OIDC endpoints.
                  If provided, this URL is used instead of constructing from the AuthentikServer's host.
                  Example: http://my-server:7000
                type: string
              clientId:
                description: |-
                  clientId is the OAuth2 client ID to use.
                  If provided, this client ID will be used instead of the randomly generated one.
                type: string
              clientType:
                default: confidential
                description: clientType is the OAuth2 client type (confidential or
                  public)
                enum:
                - confidential
                - public
                type: string
              name:
                description: name is the display name for the OAuth2 client in Authentik
                type: string
              redirectUris:
                description: redirectUris are the allowed redirect URIs for the client
                items:
                  type: string
                minItems: 1
                type: array
              scopes:
                default:
                - openid
                - profile
                - email
                description: scopes are the allowed OAuth2 scopes
                items:
                  type: string
                type: array
              secretName:
                description: |-
                  secretName is the name of the secret to create with OIDC credentials.
                  Defaults to <client-name>-oidc-credentials
                type: string
              serverRef:
                description: serverRef references the AuthentikServer to create the
                  client on
                properties:
                  name:
                    description: name is the name of the resource
                    type: string
                  namespace:
                    description: namespace is the namespace of the resource
                    type: string
                required:
                - name
                - namespace
                type: object
              subjectMode:
                default: hashed_user_id
                description: |-
                  subjectMode is the subject mode for the OAuth2 provider.
                  Determines how the subject (user identifier) is generated in tokens.
                  Valid values: hashed_user_id, user_id, user_uuid, user_username, user_email, user_upn
                  Defaults to hashed_user_id if not specified.
                enum:
                - hashed_user_id
                - user_id
                - user_uuid
                - user_username
                - user_email
                - user_upn
                type: string
            required:
            - name
            - redirectUris
            - serverRef
            type: object
          status:
            description: status defines the observed state of AuthentikClient
            properties:
              applicationId:
                description: applicationID is the Authentik application slug
                type: string
              clientId:
                description: clientID is the OAuth2 client ID
                type: string
              conditions:
                description: conditions represent the current state of the AuthentikClient
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              providerId:
                description: providerID is the Authentik provider ID
                type: integer
              ready:
                description: ready indicates whether the OIDC client is ready
                type: boolean
              secretName:
                description: secretName is the name of the secret containing OIDC
                  credentials
                type: string
            required:
            - ready
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
