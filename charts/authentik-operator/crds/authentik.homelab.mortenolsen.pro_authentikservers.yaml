---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: authentikservers.authentik.homelab.mortenolsen.pro
spec:
  group: authentik.homelab.mortenolsen.pro
  names:
    kind: AuthentikServer
    listKind: AuthentikServerList
    plural: authentikservers
    singular: authentikserver
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AuthentikServer is the Schema for the authentikservers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of AuthentikServer
            properties:
              host:
                description: host is the external hostname for Authentik
                type: string
              image:
                default: ghcr.io/goauthentik/server:latest
                description: image is the Authentik container image to use
                type: string
              postgresSecretRef:
                description: |-
                  postgresSecretRef references a secret containing the PostgreSQL connection URL.
                  The secret must have a key (default: "url") with the connection string.
                properties:
                  key:
                    default: url
                    description: key is the key in the secret to use. Defaults to
                      "url" for postgres secrets.
                    type: string
                  name:
                    description: name is the name of the secret
                    type: string
                  namespace:
                    description: namespace is the namespace of the secret. If not
                      specified, uses the same namespace as the resource.
                    type: string
                required:
                - name
                type: object
              replicas:
                default: 1
                description: replicas is the number of Authentik server pods
                format: int32
                minimum: 1
                type: integer
              tls:
                description: tls configures TLS for the Ingress
                properties:
                  enabled:
                    default: false
                    description: enabled specifies whether TLS should be enabled
                    type: boolean
                  secretName:
                    description: secretName is the name of the secret containing TLS
                      certificate and key
                    type: string
                required:
                - enabled
                type: object
            required:
            - host
            - postgresSecretRef
            type: object
          status:
            description: status defines the observed state of AuthentikServer
            properties:
              bootstrapSecretRef:
                description: bootstrapSecretRef is the name of the secret containing
                  bootstrap credentials
                type: string
              conditions:
                description: conditions represent the current state of the AuthentikServer
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              ready:
                description: ready indicates whether the Authentik server is ready
                  to accept connections
                type: boolean
              url:
                description: url is the external URL of the Authentik server
                type: string
            required:
            - ready
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
